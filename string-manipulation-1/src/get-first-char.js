/* exported getFirstChar */

// Returning the value of the first character in a string.
// Define a function named getFirstChar with one parameter of string.
// To get the first character of the string we must access the first index, which is [0].
// Return the value.

function getFirstChar(string) {
  return string[0];
}

// Returning the value of the first character in a string.
// Define a function named getFirstChar with one parameter of string. - Completed
// To get the first character of the string we must access the first index, which is [0]. - Completed
// Return the value. - Completed
