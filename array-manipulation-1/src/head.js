/* exported head */

// Finding the value of the first element of an array.
// Define a function named head with one parameter of array.
// Array indexes begin at 0, so that's where the value of the first element would be found.
// Return the first element in the array by utilizing bracket notation to indicate the index,
// in this case it's 0.

function head(array) {
  return array[0];
}

// Finding the value of the first element of an array.
// Define a function named head with one parameter of array. - Completed
// Array indexes begin at 0, so that's where the value of the first element would be found. - Completed
// Return the first element in the array by utilizing bracket notation to indicate the index,
// in this case it's 0. - Completed
