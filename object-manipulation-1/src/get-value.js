/* exported getValue */

// Obtain the value in the object from the key property.
// Define a function named getValue with two parameters of object and key.
// Return the object's key value by using bracket notation.

function getValue(object, key) {
  return object[key];
}
